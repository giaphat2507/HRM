@model IEnumerable<QLNS.Models.TonGiao>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenTonGiao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenTonGiao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaTonGiao.Trim() }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaTonGiao.Trim() }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaTonGiao.Trim() })
        </td>
    </tr>
}

</table>
